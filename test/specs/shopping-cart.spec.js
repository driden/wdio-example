describe("Shopping Cart", () => {
  it("should be present on the Homepage", () => {});
  it("should animate in when hovering on the Cart 'dropdown'", () => {});
  it("should be empty by default", () => {});
  it("should animate in when hovering on the Cart 'dropdown'", () => {});
  it("should update its data when adding an item to the cart", () => {});
});
