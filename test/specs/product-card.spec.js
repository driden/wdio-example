describe("Product card", () => {
  it("should have a title, a price, and an image associated", () => {});
  it("when hovered on, it should show 'Add to Cart' and 'More' buttons", () => {});
  it("when selecting 'Add to Cart' from the menu it should display a new dialog ", () => {});
});
